jasypt:
   encryptor:
      password: ${jasyptpwd:secretkey}
      algorithm: PBEWithMD5AndDES

server:
   port: 8000
   servlet:
      context-path: /certificaciones
      session:
         timeout: 21600s

spring:
   datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      type: com.zaxxer.hikari.HikariDataSource
      jdbcUrl: jdbc:mysql://localhost:3306/certificaciones
      username: root
      password: root
      hikari:
         poolName: pool
   flyway:
      baselineOnMigrate: true
      